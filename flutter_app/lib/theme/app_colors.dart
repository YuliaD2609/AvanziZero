import 'package:flutter/material.dart';

class AppColors {
  // Colori Primari (Verde Salvia / Teal)
  static const Color primary = Color(0xFF5A9E87);         // Verde Salvia Intenso
  static const Color primaryDark = Color(0xFF056C3F);     // Accento Verde Scuro / Teal
  static const Color primaryLight = Color(0xFFD1FAE5);    // Menta Chiaro
  static const Color primaryGradientEnd = Color(0xFF76B59D);
  static const Color primaryVariant = Color(0xFF6BB099);
  static const Color primaryDarker = Color(0xFF065F46);   // Badge scuro

  // Colori Testo
  static const Color textPrimary = Color(0xFF1C3D32);     // Verde Foresta Scuro
  static const Color textSecondary = Color(0xFF789088);   // Salvia Desaturato
  static const Color textHint = Color(0xFFB8B6AF);        // Grigio hint text

  // Colori Sfondo e Superficie
  static const Color background = Color(0xFFFBFBF9);      // Avorio soft
  static const Color surfaceLight = Color(0xFFF3F4F6);    // Sfondo grigio chiaro

  // Colori Bordo
  static const Color border = Color(0xFFEAECE8);          // Bordo standard
  static const Color borderLight = Color(0xFFF2F3F0);     // Bordo chiaro

  // Colori di Stato (Feedback, Errori, Avvisi)
  static const Color error = Color(0xFFEF4444);           // Rosso
  static const Color errorLight = Color(0xFFFEE2E2);      // Rosso chiaro
  static const Color success = Color(0xFF10B981);         // Verde successo check
  
  static const Color warning = Color(0xFFF59E0B);         // Giallo
  static const Color warningLight = Color(0xFFFEF3C7);    // Giallo tenue
  static const Color warningDark = Color(0xFFD97706);     // Giallo/Arancio scuro
  static const Color warningAlt = Color(0xFFEAB308);      // Giallo Ambra
  static const Color warningBorder = Color(0xFFFDE047);   // Giallo bordo
  static const Color warningIcon = Color(0xFFCA8A04);     // Giallo icona
  static const Color warningTextDark = Color(0xFF854D0E); // Giallo scuro testo
  static const Color warningTextMedium = Color(0xFFA16207); // Giallo medio testo

  // Ombre
  static const Color shadowLight = Color(0x051C3D32);     // Ombra leggera 5%
  static const Color shadowMedium = Color(0x081C3D32);    // Ombra media 8%
  static const Color shadowNavbar = Color(0x0A1C3D32);    // Ombra navbar 10%
  static const Color shadowCard = Color(0x0D1C3D32);      // Ombra card 13%
  static const Color shadowDark = Color(0x1A1C3D32);      // Ombra scura 26% (1A)
}
